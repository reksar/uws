---
- name: "Media"
  hosts: "all"

  tasks:

  # TODO: Check if GUI installed.
  - name: "/!/ Media packages"
    package:
      name:
        - mpv
        - yt-dlp
    become: true

  # Use `dla <URL>` to download MP3.
  - name: "Aliases for YouTube downloading"
    config_option:
      path: "{{ ansible_env.HOME }}/.bashrc.auto"
      line: >
        alias dla='yt-dlp --extract-audio --audio-format mp3 --audio-quality 0'
