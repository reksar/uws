- name: "Install QEMU"
  package:
    name:
      - "qemu-desktop"
  become: true

- name: "/>/ qemu runner"
  copy:
    src: "qemu.py"
    dest: "/usr/local/bin/qemu"
    mode: "u=rwx,g=rx,o=rx"
  become: true
