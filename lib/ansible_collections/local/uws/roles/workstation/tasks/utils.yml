- name: "/!/ System CLI utils"
  package:
    name:

      - "patch"
      - "sshfs"
      - "strace"
      - "wget"
      - "tree"
      - "pass"

      # The unified interface for all archives
      - "atool"
      # requirements:
      - "7zip"

  become: true
