# Install some CLI utils
- package:
    name:

      - "patch"
      - "sshfs"
      - "strace"
      - "wget"

      # The unified interface for all archives
      - "atool"
      # requirements:
      - "7zip"

  become: true
