# NOTE: Mind the 'meta/main' dependencies.
- name: "/!/ System packages for console workstation"
  package:
    name:

      - "base-devel"

      # Useful CLI utils
      - "patch"
      - "sshfs"
      - "strace"

      # The unified interface for all archives
      - "atool"
      # requirements:
      - "7zip"

  become: true
