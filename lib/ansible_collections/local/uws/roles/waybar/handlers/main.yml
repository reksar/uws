- name: "Restart waybar"
  shell: "killall waybar"
  register: kill
  failed_when: kill.rc > 0 and ('no process found' not in kill.stderr)
  notify: "Start waybar"

- name: "Start waybar"
  vars:
    cfg: "{{ config_dir }}/{{ config_name }}"
    css: "{{ config_dir }}/{{ style_name }}"
  shell: "nohup waybar --config '{{ cfg }}' --style '{{ css }}' &"
