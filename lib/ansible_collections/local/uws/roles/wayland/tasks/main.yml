- name: "/!/ Wayland packages"
  package:
    name:
      - "wayland"
      - "wl-clipboard"
      - "xdg-user-dirs"
  become: true

- import_tasks: "app/{{ settings.desktop.terminal }}.yml"

# FIXME: `make` fails due to the 'convert' dependency
#- import_tasks: "app/colorpick.yml"
