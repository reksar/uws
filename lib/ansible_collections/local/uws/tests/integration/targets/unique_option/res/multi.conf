# This file contains multiple aliases with the same name.
# But the only one (last) alias is used.
alias ll='ls -l --color=auto'
alias ll='ls -la'

# This alias should be present.
alias ll='ls -la --color=auto'
